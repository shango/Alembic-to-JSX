# Alembic to JSX Converter Dependencies
numpy>=1.21.0
imath>=3.1.0

# Note: Alembic Python bindings installation
# The official alembic package can be installed via:
# - pip install alembic (for database migrations - NOT what we need)
# - Building from source: https://github.com/alembic/alembic
# - Using conda: conda install -c conda-forge alembic

# For PyInstaller builds
pyinstaller>=5.0
